{
  "name": "fast58",
  "main": "index.ts",
  "type": "module",
  "devDependencies": {
    "@napi-rs/cli": "^3.6.0",
    "@types/bun": "^1.3.11"
  },
  "scripts": {
    "test": "bun test",
    "bench": "bun run benchmark.ts",
    "build:native": "napi build --platform --release"
  },
  "dependencies": {
    "@scure/base": "^2.0.0",
    "bs58": "^6.0.0"
  }
}
